import { makeStyles } from "@material-ui/core";

export const styles = makeStyles({
  header: {
    paddingBlock: 30,
    borderBottom: "1px solid #c4c4c4",
  },

  title: {
    textAlign: "center",
    fontSize: 26,
    fontWeight: 700,
  },
});
